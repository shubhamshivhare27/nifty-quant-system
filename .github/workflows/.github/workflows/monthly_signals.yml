name: Monthly Signal Engine
on:
  schedule:
    - cron: '30 15 * * 5'
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python month_end_check.py
      - env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_PASS: ${{ secrets.GMAIL_PASS }}
          RECIPIENT:  ${{ secrets.RECIPIENT_EMAIL }}
          REPORT_TYPE: monthly
          STREAMLIT_URL: ${{ secrets.STREAMLIT_URL }}
        run: python email_report.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add signals/ master_lists/ || true
          git diff --staged --quiet || git commit -m "Monthly signals $(date +'%Y-%m')"
          git push || true
